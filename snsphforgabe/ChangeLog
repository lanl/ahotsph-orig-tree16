2002-01-29  Michael Warren  <msw@s00505.nersc.gov>

	* sph.c (update_final): adjust limiters, use Msg_do() instead of
	SeriousWarning() to clean up the output file.

2002-01-28  Michael Warren  <msw@s00505.nersc.gov>

	* sn.c (sn_gravity): change smoothing function a bit, and fuzz
	the mass distribution over multiple bins
	(EPS): increase EPS to 1e-4 from 5e-5

2002-01-10  Michael Warren  <msw@s00513.nersc.gov>

	* eos3.f (rooteta): Fix eta convergence problem

	* sleos.f (EOSLOG): comment out warning message

2002-01-07  Michael S. Warren  <msw@dragon>

	* sph.c (update_intermediate): Initialize neut_out, since etanue is
	used to seed the iterative solver.

	* sphinit.c (SPHRead): read etanue
2002-01-07  Michael Warren  <msw@s00505.nersc.gov>

	* eos3.f (rooteta): increase etamax to 500.0.  50.0 seems to lead
	to trouble at small radii

2001-10-24  Michael S. Warren  <msw@ii.lanl.gov>

        * sn.c (movebound): remove *icore==1 if statement, since it was
        commented out in the fortran code

2001-10-23  Michael S. Warren  <msw@ii.lanl.gov>

        * integrate.c (PUpdateVd): stride error when strides are not identical

2001-10-22  Michael S. Warren  <msw@ii.lanl.gov>

        * main.c (main): pos-pos_last was running out of precision at the
        edge when using floats for storage

        * sph.c (update_final): hdot calculated incorrectly in 2d

        * main.c (main): udot was being cleared of dunu term

2001-10-15  Michael S. Warren  <msw@ii.lanl.gov>

        * eos3.f (eos3): u2i was not updated with stot

2001-10-10  Michael S. Warren  <msw@ii.lanl.gov>

        * main.c (main): GNewt not set to 13.34 from csdfp

2001-02-27  Mike Warren  <msw@avalon3>

	* sn.c (eos_prev): fix temp/temprev problem

2001-01-26  Mike Warren  <msw@avalon3>

	* eos3.f (eos3): Fix temprev bug in args to rootemp3

2001-01-16  Mike Warren  <msw@avalon3>

	* Use 15th-most particle for rmaxnu calcs

